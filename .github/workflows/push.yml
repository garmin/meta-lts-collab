---
name: Push
on:  # yamllint disable-line rule:truthy
  push:
    branches: ["kirkstone"]

permissions:
  contents: read
  id-token: write
  pages: write

jobs:
  periodic:
    uses: ./.github/workflows/_periodic.yml
    with:
      run-tests: false
